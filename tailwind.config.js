/**
 * Minimal Tailwind config created as a fallback because the Tailwind CLI could not be executed
 * on this system (npm reported "could not determine executable to run").
 */
module.exports = {
  content: [
    './app/**/*.{js,jsx,ts,tsx}',
    './pages/**/*.{js,jsx,ts,tsx}',
    './components/**/*.{js,jsx,ts,tsx}'
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};


